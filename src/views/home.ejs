<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NodeJS</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <ul>
        <li><a class="active" href="/">Home</a></li>
        <li><a href="/create">Create user</a></li>
    </ul>
    <!-- <div class="form" style="width:500px; margin: 20px auto;">
        <form action="/create-user" method="POST">
            <fieldset style="padding-left:20px">
                <legend>Add new users</legend><br/><br/>
                <div class="input-group">
                    <label>Name: </label>
                    <input type="text" name="username"/>
                </div><br><br>
                <div class="form">
                    <div class="input-group">
                        <label>Email: </label>
                        <input type="text" name="email"/>
                    </div>
                </div><br><br>
                <div class="form">
                    <div class="input-group">
                        <label>City: </label>
                        <input type="text" name="city"/>
                    </div>
                </div><br><br>
                <div class="form">
                    <button>Submit</button>
                </div><br/><br/>
            </fieldset>
        </form>
    </div> -->
    <h2 class="title" style="text-align: center; padding: 15px 0;">List users</h2>
    <table id="user">
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Email</th>
            <th>City</th>
            <th>Action</th>
        </tr>
        <!-- <% for(var i=0; i<listUsers.length; i++) {%>
            <tr>
                <td><%= listUsers[i].id%></td> 
                <td><%= listUsers[i].name%></td>
                <td><%= listUsers[i].email%></td>
                <td><%= listUsers[i].city%></td>
                <td>
                    <button>Edit</button>
                    <button>Delete</button>
                </td>
            </tr>
        <% } %> -->

        <% listUsers.forEach(function(user) { %>
            <tr>
                <td><%= user.id%></td> 
                <td><%= user.name%></td>
                <td><%= user.email%></td>
                <td><%= user.city%></td>
                <td>
                    <!-- a là method get, muốn dùng method khác thì bọc thẻ form -->
                    <a href="/edit/<%= user.id%>">Edit</a>
                    <form action="/delete/<%= user.id%>" method="POST">
                        <button>Delete</button>
                    </form>
                </td>
            </tr>
         <% }); %> 
    </table>
</body>

</html>